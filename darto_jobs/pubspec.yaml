name: darto_jobs
description: Background job queue for the Darto ecosystem — enqueue, retry with backoff and process jobs, backed by an in-memory or Redis store (at-least-once).
version: 1.0.0
homepage: https://github.com/evandersondev/darto/tree/main/darto_jobs
repository: https://github.com/evandersondev/darto

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  redis: ^4.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
