name: example_auth_jwt
description: Darto v2 — JWT authentication example
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  darto:
    path: ../../darto
  darto_auth:
    path: ../../darto_auth

dev_dependencies:
  lints: ^5.0.0
