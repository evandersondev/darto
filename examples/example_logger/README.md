# example_logger

What it demonstrates: Global request logging with darto_logger.

## Features
- `app.use(logger())` — logs every request with method, path, and response time
- Works automatically for all routes

## Run
```bash
dart run bin/main.dart
```
